# Environment variables & secrets
.env
.env.*
!.env.example

# Node modules & dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Databases & local state
snapshots.db
snapshots.db-journal
snapshots.db-wal
snapshots.db-shm

# Logs
*.log
bot.log
bot.log.*

# Datasets & temporary exports
dataset.csv
# CSV del experimento de filtrado del grupo de control (scripts/experiment-control-filter.js):
# se regenera desde la BD, no debe versionarse igual que dataset.csv
dataset_con_regla.csv
model_candidate.json

# Probe / scratch files
probe*.png
probe*.html
probe*.json

# OS & IDE metadata
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/

# Unrelated temporary folders
ai-job-search/
curso-basquet-tizayuca/
pmbtc/

# Cache del backtest de umbrales (regenerable, pesado)
draw_universe_*.json

# Respaldos locales: snapshots_pre_vacuum_*.db pesa ~17 GB y model_pre_*.json
# son copias de seguridad antes de reentrenar. Nunca deben entrar al repo.
snapshots_pre_vacuum_*
model_pre_*.json
.bot.lock
